// Analytics — tracking helpers (client-side / server-side)
export function recordPageView(path: string, userId?: string) {
  // TODO: record page view
}

export function recordAction(
  action: string,
  meta: Record<string, unknown> = {},
) {
  // TODO: record user action
}
